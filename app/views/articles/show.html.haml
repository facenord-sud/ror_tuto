%p#notice= notice

%p
  %b Title:
  = @article.title
%p
  %b Text:
  = @article.text

= link_to 'Edit', edit_article_path(@article), class: 'button warning small'
- if @article.comments.any?
  %h2 Comments
  = render @article.comments
- else
  %h3 No comments
%h2 Add a comment

= render 'comments/form'
